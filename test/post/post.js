// require('isomorphic-fetch')
// var base = 'http://localhost:8888'

module.exports = function postTests (test) {
    test('should refuse a post by someone youre not following', t => {
        console.log('todo post test')
        t.pass('ok')
        t.end()
    })
}
